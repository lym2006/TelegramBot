# Changelog

本项目的所有重要更改都将记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

暂无

---

## [0.4.2] - 2026-09-25

### ✨ Added

- **单实例守卫**：一台机器同时只许一个实例——启动器双击时先探测实例锁，已被占用直接弹「机器人已在运行」退出；主程序启动时持锁兜底，杜绝双开互踩配置与 Telegram 长轮询。
- **版本检查结果指引**：GUI「检查更新」发现新版时提示"重启点『是』自动更新，失败则 Releases 手动下载"，不再只报版本号。

### 🔧 Changed

- **弹窗随时可关**：网络诊断窗与版本检查等待窗不再强制等待后台探测——关窗即关，在跑线程静默收尾，最坏秒级自毁。
- **注释分区统一**：全仓整行注释补前置空行，规范写明三类豁免（连续组、块首后、docstring 后）。

---

## [0.4.1] - 2026-09-25

### ✨ Added

- **构建缓存**：嵌入式 Python 与 `get-pip.py` 首次下载后存入 `dist\_cache`，重复构建命中缓存免下载；`launcher.py` 源码指纹未变时自动复用已编译启动器，exe 哈希不随无关构建漂移。

### 🔧 Changed

- **下载全线镜像优先**：构建原料（嵌入式 Python、pip 引导脚本）与升级发布物统一改为国内镜像优先、官方源兜底，不开代理也能构建。
- **移除蓝奏云备用渠道**：发布统一走 GitHub Releases 单一来源，手动下载改为提供 gh-proxy 加速镜像直链；升级失败不再弹窗引导手动下载，仅进度窗打印原因并按当前版本启动，维护者免去蓝奏云同步上传步骤。
- **下载防半截**：构建下载先落 `.part` 再原子改名，中断不残留半成品污染缓存；pip 引导脚本逐源校验内容确为脚本，镜像返回错误页即换源。

---

## [0.4.0] - 2026-09-24

### ✨ Added

- **升级镜像源优先**：启动器下载发布物改为公共加速镜像优先、GitHub 官方兜底，国内升级不再卡官方直连。
- **启动器自动更换**：升级时新壳与当前 exe 内容比对，有变化则暂存包根，下次启动改名换入后自动重启一次；启动器修复从此可随升级分发，无需用户手动整包。存量 0.3.x 用户此次仍需手动整包一次。

---

## [0.3.3] - 2026-09-23

### 🐛 Fixed

- **代理边界越权**：安装期注入的系统代理环境变量不再泄漏给主程序——启动器拉起时显式剔除 `HTTP_PROXY`/`HTTPS_PROXY`，主程序始终按自己的三级解析选路。
- **僵尸常量**：删除 `_css.py` 中定义后从未引用的 `PRISM_CORE_FILE`、`PRISM_CORE_URL`。
- **文案换行**：校验等待窗倒计时与「网络诊断」引导提示改为换行显示，长文案不再挤一行。

### 🔧 Changed

- **日志目录单一来源**：GUI 不再以 `ROOT_DIR` 重复拼路径，统一走 `utils` 门面的 `LOGS_DIR`。
- **packaging 常量规范**：魔法数字全部提为顶部常量并标注换算单位；模块级私有常量统一私有下划线前缀（packaging 两脚本与 src 内类型别名、内部配置共 37 处），跨模块门面对外名保持裸大写，对齐 example.py 模板约定。

---

## [0.3.2] - 2026-09-23

### 🔧 Changed

- **pip 换源链扩展**：依赖安装从清华+官方两级升级为清华→阿里→腾讯→官方四级顺序换源，单源失败自动试下一个。
- **嵌入式 Python 四源轮换**：构建原料在官方与 npmmirror 之外新增华为云、淘宝镜像，全部实测可达并逐条目校验。
- **换源提示防吓**：换源/回退输出与红色报错块以空行隔离，明示"上方报错无需处理，正在自动切换镜像源"，README 与教程同步该说明。
- **失败弹框去噪**：环境安装失败不再回显 pip 命令行参数，只提示查看进度窗口末尾输出。
- **文档与注释**：文档格式规范化，删除废话。

---

## [0.3.1] - 2026-09-23

### ✨ Added

- **打包工具链**：`packaging/` 新增启动器与构建脚本——首启自装嵌入式 Python、pip、依赖与浏览器内核，在线版本页比对确认后整包自更新，用户配置数据日志一律保留；构建采用 PyInstaller onedir 与无压缩策略降低杀软误报。
- **启动进度窗口**：安装与升级全程弹出控制台，[1/5] 至 [5/5] 步骤与 pip 输出实时可见，升级下载单行刷新百分比、已下载量与速率，主程序拉起后自动回收。
- **下载多源容错**：升级发布物 GitHub 直连失败自动切换加速镜像，每个源下载后校验 zip 完整性。
- **国内镜像加速**：pip 依赖走清华源并以官方源兜底，浏览器内核走 npmmirror。
- **系统代理透传**：安装期读取注册表系统代理注入环境变量，pip 与内核下载随系统代理走。
- **构建完整性校验**：嵌入式 Python 原料双源下载并逐条目校验 zip，截断包无法出厂。
- **升级失败引导**：自动升级下载失败时弹窗询问，确认后打开下载页手动获取，按当前版本继续启动不闪退。
- **备用下载入口**：README 与启动器接入蓝奏云备用下载页（密码 5rp0），GitHub 慢时一键可达。
- **打包教程**：`packaging/packaging.md` 记录发布原理、构建参数、故障排查与蓝奏云同步上传步骤，README 维护者资源挂链。
- **构建依赖**：`pyproject.toml` dev extras 锁定 `pyinstaller==6.22.3`。

### 🐛 Fixed

- **弹窗大小**：使检测更新的文字能显示完整。
- **坏包崩溃**：发布物截断导致首启报 `File is not a zip file`；构建侧校验拦截，启动器侧给出友好重下提示。
- **升级覆盖报错**：更新流程对文件条目误用目录复制导致 `WinError 267`，已按目录与文件分流覆盖。

### 🔧 Changed

- **README 结构**：快速开始（打包版五步）置顶，开发版搭建独立成章。
- **构建自检**：`--check` 打印本地版本号、远端 tag、本地 zip、在线版本页四项；版本页由维护者手动 push，Pages 自动部署。

---

## [0.3.0] - 2026-09-22（临时跳板版，首个正式打包版本为 0.3.1）

### ⚠️ Breaking Changes（重大更新）

- **GUI 桌面化**：入口从纯控制台变为 `PySide6` 窗口，启动、配置、关闭确认全在窗口内完成；关窗弹确认，弃用 `Ctrl + C`。
- **启动流水线**：文件初始化 → 版本检测 → 配置加载 → 类型/区间校验 → 连通性三级解析，全部通过才起引擎；致命错误弹窗后安全退出，不再闪退。
- **架构分层**：主程序拆分为 `BotService` 引擎层与 `BotManager` 调度层；GUI 与 Bot 只经 `gui_bridge` 信号单向通信；异常统一收敛到 `error_guard` 守卫。

### ✨ Added

- **桌面管理面板**：
  - 仪表盘实时日志：按语义自动标 emoji，跨线程安全桥接。
  - 强制配置向导：校验失败弹出，出错字段标红、页签加 `⚠`、悬停看原因，复验通过自动关窗。
  - 弹窗族：变更二次确认、退出确认、致命提示、网络诊断、模态忙碌等待（转圈 + 秒级计时 → 结果确认；裸启动的静默校验延迟 1.2 秒弹同款窗、结果到达自动关闭）、通用提示小窗（替代原生消息框，全局配色统一）；致命/提示窗按钮默认聚焦，Enter 即确认。
- **配置校验**：
  - 类型与区间：int 自动转 float、空项丢弃；数值越界当场拦截（温度 0~2、超时 1~600 秒、间隔 0.1~720 小时）。
  - 就绪门控：加载与校验期间锁止编辑入口，杜绝未验证配置写回磁盘。
- **三级代理解析 + 端口提示**：按 配置代理 → 系统代理 → 直连 依次 getMe，先通者生效，日志标注生效通道与耗时；三级全不通才并发探测本机常用代理端口，实测可通的仅提示可填地址、不自动采用、不写配置（控制权归用户）；全不通才聚合报错并引导网络诊断；配置值坏但被后续通道救活时记错误提醒自改、不阻断启动。
- **校验分流与提速**：保存时按字段变化决定校验范围——仅 token 变复用生效通道单点复查不重跑候选链；其他字段改动只做本地类型校验；探测超时收紧（单通道 7s、连接 5s、端口探活 0.8s 并发）；校验全程弹转圈等待窗并秒级计时，通过/失败即时收口。
- **网络诊断面板**：系统代理、本地端口、Telegram 可达性逐项检测，灰（等待）→白（检测）→绿/红（结果）流式点亮；配置验证一通过原地刷绿，不重跑探测。
- **信号系统**：`SafeSignal` 防重连包装（按 tag 去重、跨线程排队投递），`GUIBridge` 中介全部 GUI↔Bot 通信。
- **生命周期管理**：`lifecycle` 统一注册同步/异步钩子与后台线程，退出逆序清理、跳过自 join、协程超时自动 cancel。
- **Token 本地预检**：格式非法（缺冒号等）不联网直接标记；代理故障时同时标注代理与 Token 两项待检查。

### 🔧 Changed

- **日志体系**：
  - 级别收敛为 INFO/ERROR/DEBUG，按内容语义注入 emoji 标记。
  - 仪表盘与 `bot.log` 只留极简可读提示，堆栈等明细统一进 `debug.log`。
- **保存流程**：改为信号驱动，校验通过才关窗；任何取消路径面板与已填内容原样保留；编辑面板与向导行为完全一致。
- **变更确认弹窗**：两级 diff 视图（行级对齐 + 行内字符级细化），删红划线、增绿加粗、未变白色；单元格自适应高 + 滚动条。
- **代码规范**：docstring 统一「摘要 + 空行 + 正文」三段式；注释精简合并；界面尺寸/颜色/字号全部收入 `_theme`（按核心/弹窗拆包）与 `_qss`，界面代码零魔法值。
- **帮助菜单图**：启动前预热渲染 + 进程内缓存复用，多人并发零重复渲染；渲染移入线程池不再阻塞事件循环。
- **报错口径**：AI 对话等前端回复不再透出异常细节，具体原因一律进后端日志。
- **utils 纯化**：工具层零日志、零 gui/bot 引用——`lifecycle.shutdown_all` 与 `plugins_register.register_routers` 改返回失败/结果数据由 bot 层记日志；异常守卫 `error_guard` 迁至 `src/bot/`；候选拼装与单通道探测剥为私有纯函数；新增 `scan_proxy_ports` 端口探活工具。详见 `docs/架构规范.md`。
- **诊断面板**："备选通道"改名"其他可通通道"，结论改为直接给出可填入代理配置的地址。

### 🐛 Fixed

- **信号串扰**：信号包装未注册导致全部信号共享底层实例（关闭程序与配置弹窗互相干扰）。
- **状态机死循环**：强制向导保存后无限重弹、无法退出。
- **校验挂死**：非法代理地址使校验协程卡死，后续所有校验停摆；代理留空被当非法代理构造（改为真·直连）。
- **退出链路**：修复 stop_polling 协程从未被 await、资源注册互斥等多处清理缺陷。
- **向导逃逸**：退出确认取消/关闭连带销毁向导、Esc 键、二次确认关窗——确认框以向导为父级全程保活，向导标题栏关闭按钮禁用；任一路径校验失败自动关编辑面板并拉起强制向导标红同一错误。
- **误弹向导**：插件全部缺失误弹无从修复的配置向导（改为致命弹窗退出）。
- **重复操作**：日志目录已开时置前现有窗口而非再开一个；版本检查移入后台线程并配模态转圈等待窗（旧实现 `asyncio.run` 卡死界面最坏 6 秒）；列表配置保存时丢弃空项，防触发词误命中全部消息。
- **校验自反馈**：校验器回读上次解析出的生效通道当成配置值，清空代理也永远"校验通过"（拆为配置原值读取与生效通道读取两路，校验只看前者）。
- **EDIT 校验失败无反馈**：编辑面板复验失败时转圈永不解除且强制向导被单窗守卫拦截（校验失败自动关闭 EDIT 切换为向导并标红出错字段）。
- **退出崩溃**：`Cannot close a running event loop`（清理改为投递 stop 后 join 循环线程再关闭）。

### 🗑️ Removed

- **控制台直跑入口**：纯命令行启动方式并入 GUI 主程序。
- **旧调度划分**：废除 `BotEngine` / `BotRunner`，改为 `BotService` / `BotManager`。
- **批处理脚本**：删除 `install.bat`、`start.bat`、`update.bat`，一键安装运行改由打包版启动器承担；开发环境搭建改为 README 手动步骤。

---

## [0.2.0] - 2026-08-21

### ⚠️ Breaking Changes（重大更新）
- **数据目录物理隔离**：将对话记录、帮助文档、渲染缓存及黑名单文件全部迁移至 `data/` 目录，代码与数据彻底分离。
- **插件路径变更**：重构了插件系统的注册与加载逻辑，旧版路径可能失效。
- **初始化流程重构**：新增 `data/` 目录自动初始化机制，首次运行将自动创建必要的数据存储结构。
- **移除旧版中间件**：废弃原有的全局鉴权中间件，所有鉴权在 `AI` 插件内部完成。

### ✨ Added
- **现代化类型系统**：全面引入 `ParamSpec` 和 `TypeVar`，提升异步代码的类型提示精度。
- **Ruff 独立配置**：分离 `ruff.toml`，统一代码风格、导入排序及格式化规范。
- **全局浏览器实例**：`AI` 插件渲染引入浏览器单例，避免任务队列中反复重启浏览器导致的资源浪费。
- **动态字体度量**：基于字体本身的数据（`Metrics`）计算帮助命令的图片尺寸，而非硬编码，提升渲染适配性。
- **AI 黑名单缓存**：新增内存缓存层读取 AI 黑名单，大幅减少高频对话时的磁盘 `I/O` 开销。
- **临时文件清理**：`Bot` 启动时自动清理先前运行残留的临时文件。
- **XSS 防御**：引入 `bleach` 库对渲染后的 `HTML` 进行白名单清洗，仅放行安全标签和属性，防止恶意脚本注入。

### 🔧 Changed
- **全局工具架构重构**：
  - `SSL` 验证屏蔽工具与主程序分离。
  - 文件初始化逻辑与配置解析逻辑分离。
- **AI 插件架构重构**：
  - 核心工作层 `WorkerLoop` 独立封装，不再与业务逻辑强绑定。
  - 会话池（`user_sessions`）与任务队列彻底解耦，仅保留纯数据交互。
  - 移出运行时产生的数据记录以及所需静态资源，保持代码纯净度。
- **日志系统增强**：
  - 解耦日志逻辑，仅修改自定义 `logger` 的参数，支持更灵活的运行时调整。
  - 强制静默第三方库噪音，屏蔽非预期的 `critical` 级别输出。
- **资源与生命周期管理优化**：
  - 文件写入后强制关闭句柄，防止资源泄露。
  - 会话结束时自动清理用户锁，防止死锁或内存溢出。
  - 将截图渲染任务放入线程池，避免同步阻塞。
- **异常处理收敛**：将分散的异常定义统一迁移至 `src/utils/exceptions.py`。
- **数据内容忽略**：更新 `.gitignore` 文件。

### 🐛 Fixed
- **配置加载安全**：移除了配置加载失败时的强制 `exit()`，改为抛出异常，提高鲁棒性。
- **根目录读取**：修复了 `ROOT_DIR` 在插件内部的导入逻辑，防止循环导入。

### 🗑️ Removed
- **AI 余额查询**：由于上游接口限制及爬虫维护成本，移除余额查询及相关爬虫代码 (`balance.py`)。
- **全局黑名单**：移除了旧版全局黑名单机制，统一由 AI 模块管理。
- **鉴权中间件**：移除 `AuthMiddleware`，相关逻辑已重构或合并。

---

## [0.1.9] - 2026-08-17

### Added
- 新增自动版本检测机制，集成 `utils/check_version.py`，在启动时自动检查是否有新版本。
- 增加键盘中断（Ctrl+C）信号捕获，支持程序的优雅退出与资源释放。
- 新增操作示意图 (`assets/example.png`)，辅助用户理解使用流程。

### Changed
- 优化 Logger 配置，屏蔽 `critical` 级别的噪音输出，使日志更清晰。
- 重写 README.md 中的脚本操作指南，提升文档可读性。

---

## [0.1.8] - 2026-08-16

### Added
- 在 README 中添加了项目运行截图，为用户提供更直观的视觉引导。

### Changed
- 将 `start.bat` 中的所有控制台提示语替换为英文，避免编码错误。

---

## [0.1.7] - 2026-08-16

### Added
- 新增 `start.bat` 一键启动脚本，支持自动激活虚拟环境与启动机器人。

### Changed
- 更新 `install.bat` 与 `update.bat` 自动化脚本，优化安装与更新流程。
- 更新 `README.md` 文档，补充 `start.bat` 使用说明及环境配置指引。

### Fixed
- 修复部分模块中的相对导入路径问题，确保包上下文解析正确。
- 修正相关模块中的变量名拼写错误，解决 `ImportError` 启动异常。

---

## [0.1.6] - 2026-08-16

### Added
- 新增项目文件自动初始化与缺失检测机制 (`init_files`)
- 新增配置文件自动扫描与版本兼容性检查 (自动合并 `config.example.toml` 中的新字段)
- 新增统一的根目录路径读取模块 (`root_dir`)，解决路径引用混乱问题

### Changed
- 重构项目目录结构，优化模块导入逻辑
- 全面接入 Ruff 代码规范检查，提升代码质量
- 安装与更新脚本的提示语改为纯英文，并在 README 中补充中文翻译
- 优化 `config` 加载流程，增强异常处理与错误提示
- 更新 README 操作步骤，替换最新的 `config.example.toml` 模板
- 更新 `.gitignore` 规则，忽略更多不必要的临时文件

---

## [0.1.5] - 2026-08-15

### Added
- 新增 `install.bat` 和 `update.bat` 自动安装/更新脚本
- 新增 Ruff 配置（`pyproject.toml` 中的 ruff 工具配置）

### Changed
- 修改 `README.md` 安装说明，补充手动安装步骤和 Windows 环境准备指南
- 修改 `README.md` 有序列表编号在代码块间被重置的问题

### Fixed
- 修复 `install.bat` 因目录非空导致 `git clone` 失败的问题
- 修复 `update.bat` 未进入 `TelegramBot` 目录导致找不到 `.git` 和 `.venv` 的问题
- 统一 `install.bat` 和 `update.bat` 的启动提示信息

---

## [0.1.4] - 2026-08-14

### Changed
- 优化了项目代码中的 Emoji 使用，提升了代码注释与日志的可读性。
- 调整了 README.md 的排版结构，完善章节描述，确保术语规范。

### Fixed
- 修复了部分 Emoji 在特定环境下无法正确渲染的问题。

---

## [0.1.3] - 2026-08-13
### Changed
- 优化 README 目录结构与排版

---

## [0.1.2] - 2026-08-13
### Added
- 初始化项目更新日志 (`CHANGELOG.md`)

### Changed
- 优化 `README.md` 文档结构，新增目录索引（TOC）与 Changelog 跳转入口
- 修正文档中的格式不规范与拼写错误
- 引入 `.gitmessage` 模板，规范日常代码提交格式

---

## [0.1.1] - 2026-08-12
### Changed
- 更新 `.gitignore` 内容，修复本地配置及日志泄露的问题

---

## [0.1.0] - 2026-08-12
### Added
- 项目架构重置与初始化
- 引入 `aiogram` 框架，实现基础的 Telegram Bot 消息收发
- 引入 `.gitignore` 规范，隔离敏感配置与本地数据文件
- 完成 AI 对话的基础功能
